public with sharing class CSBD_AlertaTelefoniaController {
    /*
    @AuraEnabled
    public static Opportunity obtenerOpp(String recId){
        if (Schema.sObjectType.Opportunity.isAccessible()) {
            Opportunity opp = new Opportunity();
            opp = [SELECT Id, CSBD_Producto_MIFID__c, StageName, IsClosed FROM Opportunity WHERE Id =: recId LIMIT 1];
            return opp;
        }
        else {
            return null;
        }
    }
        */
}