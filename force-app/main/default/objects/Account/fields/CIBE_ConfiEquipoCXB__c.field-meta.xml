<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CIBE_ConfiEquipoCXB__c</fullName>
    <description>Nos dice si el número de Centro del Account es el mismo que el de el usuario loggeado</description>
    <formula>IF(
    AND(
        NOT(ISBLANK($User.CIBE_Centro__c)),
        NOT(ISBLANK(CIBE_BookCenter__r.CC_Numero_Oficina__c)),
        CONTAINS($User.CIBE_Centro__c, CIBE_BookCenter__r.CC_Numero_Oficina__c)
    ),
    true,
    false
)</formula>
    <label>Confidencialidad EquipoCXB</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
