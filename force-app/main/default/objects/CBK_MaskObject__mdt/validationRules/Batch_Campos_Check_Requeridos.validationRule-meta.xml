<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Batch_Campos_Check_Requeridos</fullName>
    <active>true</active>
    <description>Regla de validación que obliga a informar los  campos sobre  los  que  se gestionan la fecha de enmascaramiento y campo formula de validación sobre el que se comprueba si el registro ha de ser enmascarado</description>
    <errorConditionFormula>AND(CBK_CheckMaskRecord__c,
					OR(
									ISBLANK(CBK_ObjectMaskFlagField__c ),
									ISBLANK(CBK_ObjectMaskDateField__c )
					)
	)</errorConditionFormula>
    <errorMessage>Si se ha de validar si el registro es candidato de enmascaramiento, se requiere indicar los campos sobre los  que se realizará la comprobación y actualización de la fecha  de enmascaramiento.</errorMessage>
</ValidationRule>
