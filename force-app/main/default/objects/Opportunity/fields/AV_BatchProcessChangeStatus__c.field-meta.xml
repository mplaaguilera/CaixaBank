<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AV_BatchProcessChangeStatus__c</fullName>
    <description>Campo para crear indice batch de cambio automatico de opp</description>
    <externalId>false</externalId>
    <formula>IF(
ISBLANK(AV_ExternalID__c),
&apos;NoToBeProcess&apos;,
IF(
AND(
NOT(ISBLANK(AV_ExternalID__c)),
OR(
ISPICKVAL(StageName, &apos;Potencial&apos;),
ISPICKVAL(StageName, &apos;En gestión/insistir&apos;)
)
),
&apos;ToBeProcess&apos;,
AV_ExternalID__c
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Proceso Batch - Cambio Estado</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
